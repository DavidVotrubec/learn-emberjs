import { bookType } from 'quickstart/helpers/book-type';
import { module, test } from 'qunit';

module('Unit | Helper | book type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bookType([42]);
  assert.ok(result);
});
