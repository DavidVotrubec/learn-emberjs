{{book.name}} - {{book.author}}
{{#if isDetail}}
    <br>
    {{book.desc}} - {{book.year}}
    <br>
    <button type="button" class="btn btn-sm" {{action "hideDetail"}}>Collapse</button>
{{else}}
    <button type="button" class="btn btn-sm" {{action "showDetail"}}>Show more</button>
{{/if}}